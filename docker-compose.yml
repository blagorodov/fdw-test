name: fdw

services:
  php:
    build:
      context: ./docker/php
    volumes:
      - .:/var/www
    working_dir: /var/www

  nginx:
    image: nginx:1.27-alpine
    ports:
      - "80:80"
    volumes:
      - ./public:/var/www/public:ro
      # Laravel uploads live in storage/app/public; served at /storage/*. Mount outside
      # /var/www/public — nested bind under a :ro parent fails on Linux (Docker/runc).
      - ./storage/app/public:/var/www/storage-app-public:ro
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - php

  postgres:
    image: postgres:17-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: laravel
      POSTGRES_USER: laravel
      POSTGRES_PASSWORD: laravel
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
